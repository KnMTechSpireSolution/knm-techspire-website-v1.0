# Supabase Connected Script

Your `script.js` has been updated with your Supabase connection:

```txt
SUPABASE_URL=https://gvasctwdmacbgliasrqg.supabase.co
```

Upload/replace only this file in GitHub root:

```txt
script.js
```

Then test:

1. Submit feedback from your website.
2. Go to Supabase → Table Editor → testimonials.
3. New row should appear as `pending`.
4. Change `status` to `approved`.
5. Refresh your website.
6. Approved testimonial should appear publicly.

Do not expose or upload your service_role key. Only anon public key is used in this frontend script.
