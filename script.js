const revealElements=document.querySelectorAll('.reveal');const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('active');revealObserver.unobserve(entry.target)}})},{threshold:.12});revealElements.forEach(el=>revealObserver.observe(el));
const SUPABASE_URL='PASTE_SUPABASE_PROJECT_URL_HERE';const SUPABASE_ANON_KEY='PASTE_SUPABASE_ANON_KEY_HERE';const supabaseReady=SUPABASE_URL.startsWith('https://')&&!SUPABASE_ANON_KEY.startsWith('PASTE_');const testimonialForm=document.getElementById('testimonialForm');const testimonialList=document.getElementById('testimonialList');const feedbackStatus=document.getElementById('feedbackStatus');function safe(v){const d=document.createElement('div');d.textContent=v||'';return d.innerHTML}function render(items){if(!testimonialList||!items||!items.length)return;testimonialList.innerHTML='';items.forEach(i=>{const card=document.createElement('article');card.className='testimonial live-card customer-added';card.innerHTML=`<strong>“</strong><p>${safe(i.feedback)}</p><span>${safe(i.rating)} • ${safe(i.name)}${i.company?' • '+safe(i.company):''}</span>`;testimonialList.appendChild(card)})}async function loadApproved(){if(!supabaseReady)return;try{const r=await fetch(`${SUPABASE_URL}/rest/v1/testimonials?select=name,company,rating,feedback,created_at&status=eq.approved&order=created_at.desc`,{headers:{apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${SUPABASE_ANON_KEY}`}});if(r.ok)render(await r.json())}catch(e){console.warn(e)}}loadApproved();if(testimonialForm){testimonialForm.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(testimonialForm);const item={name:f.get('name'),company:f.get('company'),rating:f.get('rating'),feedback:f.get('feedback'),status:'pending'};if(feedbackStatus)feedbackStatus.textContent='Submitting feedback for approval...';try{if(supabaseReady){const r=await fetch(`${SUPABASE_URL}/rest/v1/testimonials`,{method:'POST',headers:{apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${SUPABASE_ANON_KEY}`,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(item)});if(!r.ok)throw new Error('Supabase submission failed')}else{f.set('form-name','customer-feedback');await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(f).toString()})}testimonialForm.reset();if(feedbackStatus)feedbackStatus.textContent='Thank you. Your feedback has been submitted for approval.'}catch(err){if(feedbackStatus)feedbackStatus.textContent='Unable to submit feedback now. Please try again later.';console.error(err)}})}